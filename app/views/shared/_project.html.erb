<% @projects.each do |project| %>
  <div class="container">
    <h2><%= project.title %></h2>
    <p><%= project.content %></p>
    <%= cl_image_tag project.image, height: '300' %>
  </div>
<% end %>
